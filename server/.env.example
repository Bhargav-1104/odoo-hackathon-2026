PORT=3000
DATABASE_URL=postgresql://user:password@localhost:5432/traveloop
JWT_SECRET=change-me-to-a-long-random-string-in-production
JWT_EXPIRES_IN=7d
# Set to "true" when connecting to managed Postgres (e.g. RDS) that requires TLS
DATABASE_SSL=false
